# 成员 C 内容域测试总报告

## 结论

2026-08-27 在基线 `ebf4e51796541ded6be08dfed16ef12c9d4b261c` 上完成 UC02、UC03、UC04、UC12 三层测试：Go 全量测试通过，API 阻断断言 46/46 通过，Playwright 4/4 通过。另识别 5 项产品缺口，未修改业务代码。

| 层级 | 命令 | 结果 | 证据 |
|---|---|---|---|
| 单元 | `cd backend && go test ./... -v` | PASS，0 失败 | [单元测试输出](UC02-03-04-12-unit-test-report-20260827.txt) |
| API/集成 | `tests/api/member-c-content-test.sh` | 46 PASS / 0 FAIL / 5 GAP | [API 测试输出](UC02-03-04-12-api-test-report-20260827.txt) |
| E2E | `cd frontend && npm run test:e2e:member-c` | 4 PASS / 0 FAIL | [E2E 输出](UC02-03-04-12-e2e-test-report-20260827.txt)、[HTML 报告](UC02-03-04-12-e2e-report/index.html) |
| 前端构建 | `cd frontend && npm run build` | PASS | 本地终端验证 |

完整用例、断言与追溯见 [UC02/03/04/12 测试用例](../test-cases/member-c-uc02-03-04-12.md)。

## CI 覆盖

- `backend-test`：编译并运行全仓 Go 单元测试。
- `api-test`：依次执行 UC13 和成员 C API 套件。
- `e2e-member-c`：准备 MySQL、FFmpeg、Chromium，执行成员 C Playwright 套件并上传日志、HTML、截图、视频和 trace。
- `docker-build` 同时依赖 `backend-test`、`frontend-build`、`api-test`、`e2e-member-c`；任一失败均阻断镜像构建。
- 本轮未推送远程，因此只有本地配置校验和测试证据，没有 GitHub Actions run URL；不得把远程 CI 证据标为完成。

## 已知缺口

| UC | 缺口 | 本轮处理 |
|---|---|---|
| UC02 | 非法排序返回 500；详情不检查媒体文件存在性 | API 报告记为 GAP |
| UC03 | 转码失败无失败状态和原因 | 验证不公开并记为 GAP |
| UC04 | 重复审核覆盖终态；通过时不检查实际 HLS | API 报告记为 GAP |
| UC12 | 单作品模式排行未随作品过滤 | API 报告记为 GAP |

## 环境说明

- Go：1.26.3；Node.js：24.16.0；npm：11.13.0；FFmpeg：7.0.2。
- 本机 Docker daemon 不可连接，因此本地测试使用临时用户态 MariaDB 11.8.6；CI 仍按仓库配置使用 MySQL 8.0 service。
- `npm ci` 报告现有依赖含 7 个 high severity audit 项，本轮未执行可能改变依赖版本的 `npm audit fix`。
