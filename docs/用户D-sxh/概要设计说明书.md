# 互动与直播域概要设计说明书（成员 D）

## 1. 设计范围

本文给出 UC05、UC09、UC10 的组件设计。当前系统采用 Vue 前端、Gin 单体后端、MySQL 和 SRS；计划中的 `engagement-service` 拆分不在当前运行基线中。

## 2. 组件图

```mermaid
flowchart LR
    subgraph Browser[Vue 前端]
        VP[VideoDetailPage / VideoPlayer]
        LL[LiveListPage]
        LS[LiveStudioPage]
        LR[LiveRoomPage]
        API[HTTP API 客户端]
        WSC[DanmakuWebSocket]
    end

    subgraph Backend[Gin 后端]
        Router[路由与鉴权中间件]
        VH[Video / Danmaku / Comment Handler]
        SH[Live Schedule Handler]
        LH[Live / Interaction Handler]
        WSH[Live / Publish WebSocket Handler]
        Hub[Live Danmaku Hub]
        Worker[Schedule Worker]
    end

    DB[(MySQL)]
    SRS[SRS]

    VP --> API
    LL --> API
    LS --> API
    LR --> API
    LS --> WSC
    LR --> WSC
    API --> Router
    WSC --> WSH
    Router --> VH
    Router --> SH
    Router --> LH
    WSH --> Hub
    VH --> DB
    SH --> DB
    LH --> DB
    Hub --> DB
    Worker --> DB
    LS --> WSH
    WSH --> SRS
    SRS --> LR
```

## 3. UC05 组件级设计

### 3.1 组件级顺序图

```mermaid
sequenceDiagram
    actor U as 用户
    participant P as VideoDetailPage/VideoPlayer
    participant A as HTTP API Client
    participant R as Gin Router/Auth
    participant H as Video/Danmaku/Comment Handler
    participant D as MySQL
    U->>P: 打开视频详情
    P->>A: 查询视频、弹幕、评论与互动状态
    A->>R: GET 请求
    R->>H: 分派并解析可选用户
    H->>D: 查询审核通过视频及互动数据
    D-->>P: 返回页面数据
    U->>P: 发送弹幕/评论/切换点赞收藏
    P->>A: POST 请求
    A->>R: Bearer Token + DTO
    R->>H: 鉴权后分派
    H->>D: 事务写关系和计数
    D-->>P: 返回最新对象或状态
```

## 4. UC09 组件级设计

### 4.1 组件级顺序图

```mermaid
sequenceDiagram
    actor A as 主播
    actor V as 用户
    participant P as LiveListPage
    participant API as liveApi
    participant R as Gin Router/Auth
    participant H as Schedule Handler
    participant W as Schedule Worker
    participant D as MySQL
    A->>P: 填写并发布计划
    P->>API: createSchedule
    API->>R: POST /live-schedules
    R->>H: CreateScheduleHandler
    H->>D: 事务创建计划和关注通知
    D-->>P: pending 计划
    V->>P: 点击预约
    P->>API: reserveSchedule(id)
    API->>R: POST /live-schedules/:id/reserve
    R->>H: ReserveScheduleHandler
    H->>D: 事务切换预约关系和 reminder_count
    D-->>P: reserved + reminderCount
    W->>D: 每 30 秒扫描到期 pending 计划
    W->>D: 事务更新 live 并准备直播间/通知
```

## 5. UC10 组件级设计

### 5.1 组件级顺序图

```mermaid
sequenceDiagram
    actor A as 主播
    actor V as 观众
    participant SP as LiveStudioPage
    participant RP as LiveRoomPage
    participant API as liveApi
    participant H as Live/Interaction Handler
    participant WS as WebSocket Handler
    participant Hub as Live Hub
    participant DB as MySQL
    participant SRS as SRS
    A->>SP: 开始直播
    SP->>API: createLive
    API->>H: POST /live
    H->>DB: 创建/复用房间并重置本场统计
    DB-->>SP: room + streamKey
    SP->>WS: 连接 /ws/live-publish/:id
    WS->>SRS: 转发 WebM 媒体
    V->>RP: 进入直播间
    RP->>API: detail + interaction + danmaku
    RP->>WS: 连接 /ws/live/:id
    WS->>Hub: Register(client)
    Hub->>DB: 更新 viewer_count/viewer_peak
    Hub-->>RP: viewer_count
    V->>API: 点赞/礼物
    API->>H: interaction request
    H->>DB: 事务写入并更新统计
    H->>Hub: BroadcastEvent
    Hub-->>RP: live_like/live_gift
    V->>WS: 发送弹幕
    WS->>Hub: 权限/慢速模式校验
    Hub->>DB: 保存 live danmaku
    Hub-->>RP: danmaku
```

## 6. 接口与数据边界

| 用例 | HTTP/WS 边界 | 主要数据 |
| --- | --- | --- |
| UC05 | `/danmaku/*`、`/comments/*`、`/videos/:id/like`、`/videos/:id/collect` | Video、Danmaku、Comment、Like、Collect、CommentLike |
| UC09 | `/live-schedules`、`/live-schedules/:id`、`/live-schedules/:id/reserve` | LiveSchedule、LiveReservation、Notification、LiveRoom |
| UC10 | `/live/*`、`/ws/live/:id`、`/ws/live-publish/:id` | LiveRoom、LiveLike、LiveGift、Danmaku、Follow、CreatorSubscription |

## 7. 关键设计决定

- HTTP 负责可重试的查询和事务写操作，WebSocket 负责房间实时事件。
- 预约关系、点赞关系使用用户和目标的联合唯一约束，避免产生重复有效记录。
- 房间 Hub 对已登录观众按用户编号去重，匿名观众按连接计数；监控连接显式排除。
- 媒体流与业务事件分离：SRS 处理播放媒体，Gin/Hub 处理身份、互动和统计。
- 实时弹幕采用“成功持久化后广播”；数据库写入失败时发送 `chat_error`，不产生房间广播。
