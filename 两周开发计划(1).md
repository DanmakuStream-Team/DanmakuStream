# DanmakuStream 软件工程基础实践（2026 夏）两周开发计划

> 时间：2026 年 8 月 25 日—9 月 4 日，共 10 个集中实践日（8 月 30 日为机动日）。
>
> 团队：5 人，以下用成员 A～E 占位，确定姓名后直接替换。
>
> 分支流程：`main`（验收）← `dev`（集成）← `feature/*`、`fix/*`、`docs/*`。所有合并必须走 PR，并由另一名成员 Review。

---

## 1. 最终业务范围与当前缺口

本计划以《DanmakuStream 业务场景（用例）清单》为准，共 **13 个用例**：UC01～UC10、UC12～UC14。清单中 **UC11 缺号**，第 1 天必须向教师/助教确认是编号遗漏还是缺少业务场景；确认前不得自行补写一个 UC11。

| 用例 | 业务目标 | 当前判断 | 两周内要补齐的重点 |
|---|---|---|---|
| UC01 | 注册、登录与资料维护 | 基本实现 | 权限、重名、错误密码、身份过期等自动测试 |
| UC02 | 视频发现、搜索与播放 | 基本实现 | 不可播放状态、空结果、媒体失败测试 |
| UC03 | 创作者投稿与状态跟踪 | 基本实现 | 取消上传、非法文件、转码失败及状态一致性测试 |
| UC04 | 视频审核与发布 | 基本实现 | 重复审核、媒体不可用、审核权限测试 |
| UC05 | 视频观看互动 | 基本实现 | 幂等、非法内容、取消点赞/收藏、统计一致性测试 |
| UC06 | 个人视频资料库 | **部分实现** | 现有记录主要在浏览器本地；新增服务端历史、进度、稍后再看、清空与多端一致性 |
| UC07 | 关注关系与内容通知 | **部分实现** | 新增分组、特别关注、屏蔽及对应通知规则 |
| UC08 | 创作者会员订阅 | **新增开发** | 会员方案、订单、演示支付、有效期、自动续订及幂等 |
| UC09 | 直播预约与用户预约 | 基本实现 | 非法/冲突时间、重复预约、取消流程测试 |
| UC10 | 直播发布、观看与实时互动 | **部分实现** | 新增直播工作台、礼物/Super Chat、重连与在线人数去重 |
| UC12 | 用户私信与媒体分享 | **新增开发** | 会话、文字/媒体消息、实时通知、历史、已读与未读数 |
| UC13 | 创作者数据分析 | **新增开发** | 指标汇总、按日趋势、权限和空数据降级 |
| UC14 | 审核、权限、运营与基础设施管理 | 基本实现 | 权限边界、指标来源、失败状态和管理操作测试 |

> 范围风险：UC08、UC12、UC13 当前并非“已有功能补测试”，而是完整新增业务。若课程允许只选现有业务场景，应在第 1 天优先与教师确认能否将它们降为 P1；若教师确认 13 个全部必做，则按本文计划执行，并冻结其他体验优化。

## 2. 两周需要完成的六个 Feature

| Feature | 要做的内容 | 量化验收 |
|---|---|---|
| F1. 13 个业务用例闭环 | 补齐 UC06/07/08/10/12/13，回归其余用例；每个主责人同时负责后端、自己的业务页面、文档和测试 | 教师确认的用例全部通过；当前目标 13/13 |
| F2. 文档与追溯 | 每个用例完成说明、系统/组件/对象级图、代码与测试映射 | 13 份说明、39 张三层图、1 张总用例图、改造前后部署图、追溯率 100% |
| F3. 自动化测试 | 单元、API/集成、E2E 三层测试；CI 失败必须阻断部署 | 公开 API 自动测试覆盖 100%；13/13 E2E 通过 |
| F4. 容器化与三个业务微服务 | 拆分 user/content/engagement，Schema 隔离，统一网关，独立构建测试部署 | 3 个服务均可独立运行；禁止跨服务直连表 |
| F5. Kubernetes 与 CI/CD | 镜像版本、自动部署、探针、日志、资源限制、HPA、故障演练 | push 后自动部署；扩/缩容各出现一次；故障不连锁崩溃 |
| F6. 性能与课程交付 | 单体/微服务对比、看板、README、PPT、技术报告、现场演示 | 2～3 个接口，每版本至少 3 次；证据和原始数据齐全 |

## 3. 五人重新分工

### 3.1 主责与个人分支

| 成员 | 主责领域 | 主责用例/Feature | 主代码分支 | 文档与测试分支 |
|---|---|---|---|---|
| A | 组长、平台与 DevOps | UC14；F4/F5；网关、Compose、CI/CD、K8s、HPA、故障实验 | `feature/p1-platform-devops` | `docs/p1-uc14-deployment`、`feature/p1-platform-tests` |
| B | 用户与社交服务 | UC01、UC07、UC08、UC12；`user-service` | `feature/p2-user-service` | `docs/p2-user-trace`、`feature/p2-user-tests` |
| C | 内容服务 | UC02、UC03、UC04、UC13；`content-service` | `feature/p3-content-service` | `docs/p3-content-trace`、`feature/p3-content-tests` |
| D | 互动与直播服务 | UC05、UC09、UC10；`engagement-service` | `feature/p4-engagement-service` | `docs/p4-engagement-trace`、`feature/p4-engagement-tests` |
| E | 资料库、公共前端、QA 与交付 | UC06；F2/F3/F6；前端公共层、全量 E2E、压测、追溯与交付 | `feature/p5-library-frontend` | `feature/p5-e2e-benchmark`、`docs/p5-delivery` |

这不是只把业务目标分给 B/C/D：

- A 有完整业务目标 UC14，同时承担所有人都会依赖的部署平台和云原生实验。
- E 有完整业务目标 UC06，同时承担公共前端、13 个用例 E2E、压测和最终交付。
- B/C/D 的每个用例都要自己完成对应前端页面，E 不负责替所有人写页面。

### 3.2 前端代码归属

| 前端内容 | 负责人 |
|---|---|
| 登录、资料、关注、会员、私信页面 | B |
| 首页/搜索、视频投稿/审核、创作者分析页面 | C |
| 视频互动区、直播列表/房间/工作台页面 | D |
| 个人资料库页面 | E |
| 路由、请求封装、登录态、公共布局、公共类型、网关切换 | E |
| `VideoDetailPage.vue` 等多人共用页面 | C 维护页面骨架，D 维护互动组件；分别提交小 PR，避免同时改同一区域 |

### 3.3 Review 规则

- 固定 Review：A ↔ E，B ↔ C，D 的 PR 由 B 或 C Review。
- 分支连续开发不得超过 2 天；超过时按 `feature/pN-具体任务` 再拆分。
- 业务主责人必须同时提交代码、图、测试和追溯链接，不能把全部文档丢给 E。
- `main` 只保存可演示版本；`dev` 为唯一集成入口；任何人不得直接推送 `main`。

## 4. 三个业务微服务划分

| 服务 | 业务用例 | 主要数据归属 | 负责人 |
|---|---|---|---|
| `user-service` | UC01、UC07、UC08、UC12 | 用户、关注/分组/屏蔽、会员方案/订单/订阅、会话/消息/已读状态 | B |
| `content-service` | UC02、UC03、UC04、UC13，及 UC14 的内容运营部分 | 视频、媒体、审核、横幅/公告、流量统计、创作者指标 | C |
| `engagement-service` | UC05、UC06、UC09、UC10 | 点赞/收藏/评论/弹幕、历史/进度/稍后再看、直播/预约/礼物/Super Chat | D（UC06 由 E 开发） |

- A 负责 API Gateway、部署模板和三个服务的公共运行规范，但不代替领域负责人实现业务。
- 可共用一个 MySQL 实例，但必须使用 `user_db`、`content_db`、`engagement_db` 三个 Schema 和独立账号授权。
- 禁止跨服务联表；跨服务读取走内部 HTTP API，并设置超时、明确错误和降级策略。
- 网关、前端、数据库和 SRS 不计入“三个业务微服务”。

## 5. 统一工具

- **所有正式图统一使用 PlantUML**：用例图、类图、系统/组件/对象级顺序图、组件图和部署图均提交 `.puml` 源文件，并导出 SVG/PNG。
- **部署图使用 PlantUML Deployment Diagram**，至少画“改造前单体部署图”和“改造后 Kubernetes 部署图”。
- 推荐目录：`docs/models/usecase/`、`system/`、`component/`、`object/`、`deployment/`。
- 统一编号：`REQ01 / UC01 / SYS-SEQ01 / COMP-SEQ01 / OBJ-SEQ01 / UNIT-TC01 / INT-TC01 / E2E-TC01`。
- 项目看板统一使用 **GitHub Projects**，状态为 `Backlog → Ready → In Progress → Review → Done`，另设 `Blocked`。
- 每个 Issue 必须写负责人、分支、用例编号、验收条件、PR、测试/文档证据；只有证据齐全才能进入 Done。

## 6. 每天每人做什么

### 第 1 天（8/25）：冻结范围、跑通基线、完成系统级图

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 从空环境启动现系统；建立 GitHub Project、Issue 与验收字段；核验 UC14 主/异常流程；完成 UC14 说明和 SYS-SEQ14；画单体部署图；全部基线通过后打 `monolith-start` 标签 | `feature/p1-baseline-board`、`docs/p1-uc14-deployment` |
| B | 核验 UC01/07；确认 UC08/12 对应文件和接口缺失；写 UC01/07/08/12 用例说明与 SYS-SEQ01/07/08/12；列出用户域新增表/API | `docs/p2-day1-user-audit` |
| C | 核验 UC02/03/04；确认 UC13 缺口；写 UC02/03/04/13 用例说明与 SYS-SEQ02/03/04/13；列内容域接口/表 | `docs/p3-day1-content-audit` |
| D | 核验 UC05/09/10；列出 UC10 礼物、Super Chat、工作台和重连缺口；写三个用例说明与 SYS-SEQ05/09/10 | `docs/p4-day1-engagement-audit` |
| E | 核验 UC06 现有本地存储实现与服务端缺口；写 UC06 说明和 SYS-SEQ06；画总用例图；建立 13 个用例的页面/API/测试追溯表骨架 | `docs/p5-day1-library-trace` |

第 1 天共同完成条件：

1. 向教师确认 13 个用例是否全部纳入，以及 UC11 缺号原因。
2. 13 个用例都有“已实现/部分实现/新增”状态和代码证据，不凭文档猜测。
3. 完成 13 份用例说明、13 张系统级图、1 张总用例图、1 张单体部署图。
4. 系统可重复启动，测试账号、环境、端口、缺陷和看板链接入库。

### 第 2 天（8/26）：详细设计、容器化与新增功能接口冻结

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 补前后端 Dockerfile、数据库初始化/测试数据、Compose；完成 UC14 组件/对象级图；设计 CI 阶段和 K8s 部署结构 | `feature/p1-containerize` |
| B | 完成 UC01/07/08/12 组件/对象级图；冻结关系、会员、订单、订阅、会话、消息模型与 API；定义支付幂等 | `docs/p2-user-trace` |
| C | 完成 UC02/03/04/13 组件/对象级图；冻结创作者汇总/趋势 API 和数据口径 | `docs/p3-content-trace` |
| D | 完成 UC05/09/10 组件/对象级图；冻结礼物/Super Chat、直播工作台、实时消息和在线人数规则 | `docs/p4-engagement-trace` |
| E | 完成 UC06 组件/对象级图；冻结历史、进度、稍后再看、清空 API；汇总概念类图、组件图、测试计划 | `docs/p5-library-test-plan` |

当日验收：换一台机器可按 README 用 Compose 启动；39 张三层图初稿齐全；新增功能 API 不再随意变更。

### 第 3 天（8/27）：新增功能第一批 + 测试与 CI

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 建 CI：依赖安装→编译→单元/API 测试→版本镜像；制造一次测试失败，验证后续镜像/部署被阻断；补 UC14 测试 | `feature/p1-monolith-ci` |
| B | 实现 UC07 分组/特别关注/屏蔽；实现 UC08 会员方案、订单、演示支付与订阅；补核心单元/API 测试 | `feature/p2-user-social-membership` |
| C | 为 UC02/03/04 补关键测试；实现 UC13 指标汇总、趋势、权限和空数据接口 | `feature/p3-content-analytics` |
| D | 为 UC05/09 补关键测试；实现 UC10 礼物/Super Chat 后端和直播工作台基本接口 | `feature/p4-live-interaction` |
| E | 实现 UC06 服务端历史/进度/稍后再看及前端迁移；搭建 E2E 框架；更新追溯表 | `feature/p5-library-frontend` |

### 第 4 天（8/28）：补齐新增功能、全用例 E2E、单体上 K8s

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 将单体版部署到 Kind/Minikube/k3d；CI 自动部署并执行健康检查；冻结三个微服务接口、表归属和失败策略 | `feature/p1-k8s-monolith` |
| B | 实现 UC12 会话、文字/图片/短视频/平台视频分享、历史、已读/未读及实时通知；补非法附件和屏蔽测试 | `feature/p2-user-messaging` |
| C | 完成 UC13 前端图表、范围切换和权限；回归 UC02/03/04/13 | `feature/p3-creator-analytics-ui` |
| D | 完成 UC10 工作台、礼物/Super Chat、WS 重连、在线人数去重；回归 UC05/09/10 | `feature/p4-live-studio` |
| E | 完成 UC06 E2E；搭好其余 12 个 E2E 脚本骨架并要求领域负责人补断言；生成测试报告与追溯表 v1 | `feature/p5-e2e-monolith` |

### 第 5 天（8/29）：中期检查与单体版冻结

| 成员 | 当天必须完成 |
|---|---|
| A | 演示 Compose、CI 红/绿记录、K8s 单体部署、UC14；主持缺口清单和版本冻结 |
| B | 演示 UC01/07/08/12；冻结用户域代码、图、接口、测试证据 |
| C | 演示 UC02/03/04/13；冻结内容域代码、图、接口、测试证据 |
| D | 演示 UC05/09/10；冻结互动直播域代码、图、接口、测试证据 |
| E | 演示 UC06；运行并汇总 13/13 E2E；核对 39 张图与追溯表 |

当日验收：中期材料全部可从仓库和 GitHub Projects 找到。若 13 个用例全保留但新增功能尚未闭环，8 月 30 日只用于补 P0 缺口，不新增需求。

### 第 6 天（8/31）：拆分三个业务微服务

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 建 API Gateway、三服务 Compose/K8s 骨架、ConfigMap/Secret、统一健康与版本规范 | `feature/p1-microservice-platform` |
| B | 拆出 `user-service`：UC01/07/08/12，独立 Schema、Dockerfile、探针和版本接口 | `feature/p2-user-service` |
| C | 拆出 `content-service`：UC02/03/04/13 与 UC14 内容运营部分 | `feature/p3-content-service` |
| D | 拆出 `engagement-service`：UC05/06/09/10 | `feature/p4-engagement-service` |
| E | 前端统一改走网关，处理登录态和错误格式；建立微服务 E2E 环境 | `feature/p5-frontend-gateway` |

### 第 7 天（9/1）：数据隔离与跨服务主链路

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 配网关路由和三个服务独立流水线；镜像使用 commit SHA；配置最小权限数据库账号 | `feature/p1-microservice-cd` |
| B | 提供内部用户摘要、关系/屏蔽、会员状态接口；移除跨 Schema 查询 | `feature/p2-user-service` |
| C | 提供内部视频摘要、可播放性、所有权接口；移除跨 Schema 查询 | `feature/p3-content-service` |
| D | 将用户/视频查询改为内部 API，设置超时、明确失败和降级结果 | `feature/p4-engagement-service` |
| E | 联调“注册登录→搜索播放→互动→资料库→通知”跨服务链路；更新微服务部署图和追溯表 | `feature/p5-microservice-integration` |

### 第 8 天（9/2）：自动部署、可观测与全量回归

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | push 后自动构建/测试/镜像/部署；三服务日志、存活/就绪探针和版本可查；保留一次真实失败排查记录 | `feature/p1-k8s-microservice-cd` |
| B | 运行 user-service 全部公开/内部 API 测试并修复 | `fix/p2-user-regression` |
| C | 运行 content-service 全部公开/内部 API 测试并修复 | `fix/p3-content-regression` |
| D | 运行 engagement-service 全部 API/WS 测试并修复 | `fix/p4-engagement-regression` |
| E | 运行 13/13 微服务版 E2E；修复公共前端集成问题；输出测试报告和最终追溯表 | `feature/p5-e2e-microservice` |

### 第 9 天（9/3）：HPA、故障与性能对比

| 成员 | 当天必须完成 | 分支 |
|---|---|---|
| A | 配 requests/limits/HPA；压测触发 Pod 增加和回落；停止一个依赖服务做故障演练；保存指标、日志和截图 | `feature/p1-k8s-hpa-chaos` |
| B | 配合 user-service 压测与故障；验证屏蔽/会员/消息数据不重复、不丢失 | `fix/p2-benchmark` |
| C | 配合 content-service 压测与故障；核对搜索/播放接口结果和统计口径 | `fix/p3-benchmark` |
| D | 验证 engagement-service 在依赖故障时按设计超时/降级，且其他 Pod 不连锁崩溃 | `fix/p4-benchmark` |
| E | 选 2～3 个主要接口，单体/微服务同机、同数据、同脚本各测至少 3 次；整理吞吐、平均、P95、错误率、CPU/内存 | `feature/p5-e2e-benchmark` |

### 第 10 天（9/4）：最终交付与答辩

| 成员 | 当天必须完成 |
|---|---|
| A | 冻结版本；完成 README、部署/回滚说明；主控 CI/CD、K8s、HPA 和故障演示 |
| B | 重点演示 UC08 会员、UC12 私信及 user-service 数据隔离，并准备备用演示录屏 |
| C | 重点演示 UC03 投稿、UC04 审核、UC13 分析及 content-service |
| D | 重点演示 UC10 直播互动及 engagement-service 的故障处理 |
| E | 演示 UC06 跨端资料库；讲解全量 E2E、性能对比、追溯表；汇总 PPT、技术报告、AI 使用说明与个人权重 |

全组终检：两个版本和提交记录、README、Dockerfile/Compose、流水线、K8s YAML、数据库初始化与回滚、测试代码与原始报告、压测原始数据、全部模型源文件、GitHub Projects、PPT 和技术报告均可访问。

## 7. 量化测试与验收口径

| 验收项 | 固定条件 | 必须记录 | 通过标准 |
|---|---|---|---|
| 用例回归 | 固定账号和初始化数据，执行主流程及关键异常 | 总数、通过/失败、截图/日志、缺陷链接 | 教师确认范围全部通过；当前目标 13/13 |
| 文档追溯 | 每个 REQ/UC 对应图、代码、测试 | 追溯表空项数 | 13 个用例追溯率 100%，三层图 39/39 |
| 单元测试 | 各服务独立运行，覆盖关键规则和异常 | 数量、通过/失败、失败原因 | P0 规则全部通过，断言有效 |
| API/集成测试 | 固定初始化脚本；成功和主要异常均断言 | 接口总数、已测数、状态码、关键响应 | 公开 API 自动测试覆盖 100%，全部通过 |
| E2E | 从前端或公开入口执行完整业务 | 用例和流程结果 | 13/13 通过；失败必须关联 Issue |
| CI 阻断 | 一次失败提交、一次修复提交 | 两份完整流水线记录 | 测试失败时不制作/发布镜像；修复后自动部署并健康 |
| 数据隔离 | 三个 Schema 和最小权限账号 | 表归属、授权、跨服务调用 | 无跨服务直连表，越权账号不能查询 |
| HPA | 固定脚本、并发、时长和阈值 | Pod 数时间线、吞吐、P95、错误率 | 高压扩容、压力下降后缩容，可复现 |
| 故障处理 | 停止 user/content 依赖或注入延迟 | 故障时间、响应、超时、Pod、日志 | 2 秒内明确失败/降级，不无限等待、不连锁崩溃 |
| 性能对比 | 同机、同数据、同脚本、同并发 | 吞吐、平均、P95、错误率、CPU、内存、原始输出 | 2～3 接口，每版本至少 3 次；如实解释差异 |

> 内部建议目标：API 压测错误率 < 1%、跨服务调用超时 ≤ 2 秒。课程若未规定固定阈值，最终结论以原始实测数据为准，不要求微服务一定比单体快。

## 8. 每天固定动作

1. 9:00 站会：昨天完成、今天目标、当前阻塞。
2. 开工前把 Issue 从 Ready 移到 In Progress，并写当日验收条件。
3. 当天代码当天提交，小 PR 合入 `dev`；不得攒到最后一天。
4. PR 必须关联用例、Issue、测试结果和图/文档，Review 后才能合并。
5. 下班前更新追溯表、缺陷、实际工时和 GitHub Projects，并保存带日期的看板证据。
6. 任何 P0 测试红灯优先修复，禁止跳过测试发布。

## 9. 风险控制

- 13 个用例中有 3 个完整新增、3 个明显补齐项，进度风险高；第 1 天必须向教师确认范围，之后冻结需求。
- 若新增功能延期，先保主成功流程、权限和数据一致性，再做 UI 美化；不得牺牲测试、部署和可复现证据。
- 多人改同一前端页面时拆成组件，小 PR 顺序合并，避免长期分叉。
- 微服务拆分落后时，优先保三个业务服务独立构建、测试、部署和 Schema 隔离。
- 性能变慢可以如实报告并分析网络跳数和序列化开销，禁止修改或挑选数据。
- 仓库不得提交密码、Token 或真实密钥，统一使用环境变量和 Kubernetes Secret。
