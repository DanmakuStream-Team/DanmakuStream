# DanmakuStream 前端环境变量示例
# 复制本文件为 .env.development 或 .env.production 后按实际值修改。
# 变量前缀必须为 VITE_，Vite 才会注入到浏览器端。

# ── API 网关地址（浏览器端 request.ts 使用） ──────────────────────────────────
# 同域部署（默认）：留空即走前端同域 Nginx / Vite proxy 反代 → 网关
# 跨域部署（前端和网关不同域）：填绝对地址，如 http://api.example.com/api/v1
# 注意：跨域部署时需要后端开启 CORS，且 Authorization Cookie 需配置 SameSite 与 Secure。
VITE_API_BASE_URL=/api/v1

# ── 本地开发模式下，Vite dev server 反代的网关目标（仅 Node/Vite 进程使用） ──────
# docker compose 起了 nginx-gateway 时，走网关：默认 http://localhost:8888
# 不使用 compose、直接 go run 单体后端本地开发，回退直连：http://localhost:8080
VITE_DEV_GATEWAY_TARGET=http://localhost:8888
VITE_DEV_GATEWAY_WS_TARGET=ws://localhost:8888

# ── 其他项目级变量 ──────────────────────────────────────────────────────────
# 预留：推荐系统标签页 URL（若部署了独立推荐服务可填绝对地址）
# VITE_RECOMMEND_BASE_URL=http://rec.example.com
