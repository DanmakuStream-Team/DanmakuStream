apiVersion: v1
kind: Service
metadata:
  name: srs
  namespace: danmakustream
spec:
  selector: { app: srs }
  ports:
    - { name: rtmp, port: 1935, targetPort: 1935 }
    - { name: api, port: 1985, targetPort: 1985 }
    - { name: hls, port: 8080, targetPort: 8080 }
